import React from 'react';
import ReactDOM from 'react-dom'; // Imported 'react-dom' properly
import App from './App'; // Use the correct file extension '.jsx' is optional
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  
  <App /> // Pass the prop within JSX
);
